# Session Summary — 2026-04-11
## AI-Guided Theme Generation SOP Hardening

This document provides context for the next AI agent continuing this work.
Read it in full before taking any action.

---

## What Was Accomplished

The `AI-Guided-Theme-Generation.md` master runbook was hardened from a rough
draft into a production-ready 5-phase Standard Operating Procedure. All
identified logical gaps, architectural contradictions, and execution stalls
were resolved and committed individually to the `ai_guidance` repository.

---

## Repository Structure

| Path | Purpose |
|------|---------|
| `drupal/ai_guide_theming/AI-Guided-Theme-Generation.md` | Master SOP — framework-agnostic |
| `drupal/ai_guide_theming/session-2026-04-11.md` | This file |
| `drupal/dripyard_themes/color_management.md` | Dripyard-specific color rules |
| `drupal/dripyard_themes/theme_component_mapping_plan.md` | Dripyard component mapping strategy |

The master SOP is intentionally framework-agnostic. It instructs the AI to
ask the user for the base theme documentation folder at runtime (e.g.,
`drupal/dripyard_themes`). All Dripyard-specific knowledge lives separately.

---

## SOP Phase Summary

### Phase 1: Pre-Execution Discovery
Collect and display all runtime parameters before executing anything:
- Primary theme name, project path, docs namespace, screenshot location
- **Existing Site Audit**: Ask the user explicitly — *"Is there an existing version of this website running locally?"* If yes, record its path. A yes answer makes Phase 3 Step 2 **mandatory**.
- Auto-detect container runtime (scan `.ddev/`, `.lando.yml`) — never hardcode
- Run `git status` — block on dirty working tree
- Display parameters as a table, ask for user approval before proceeding

### Phase 2: Establish the Baseline Backup
- Clone stable theme to timestamped working directory
- Refactor machine names in config files ONLY (`.info.yml`, `.breakpoints.yml`,
  `.theme`, `.libraries.yml`) — never touch binary assets or CSS class names
- Activate via `[runtime_wrapper] drush`
- Commit with explicit path only — never `git add .`

### Phase 3: Screenshot Ingestion & Component Mapping
- Save screenshots to `designs/` inside the new theme
- **Legacy Audit (mandatory if existing site confirmed in Phase 1 — do not skip)**:
  Detect legacy site runtime independently, then audit: content types, menus,
  block placements, taxonomy vocabularies, active layout modules.
  Write findings to `audits/legacy_dissection.md` inside the new theme.
  This output directly drives template suggestion hooks and sidebar wiring.
- Decompose screenshot into horizontal bands
- Cross-reference against user-specified base theme component library
- Identify gaps needing bespoke SDCs
- Draft `theme_component_mapping_plan.md` into the user's docs folder
- Commit all artifacts with explicit paths — never `git add .`
- Hard stop for user approval before proceeding to Phase 4

### Phase 4: Page Template Architecture
- Inventory all page types in scope (Canvas full-width, sidebar docs, utility)
- Identify the homepage node; do not create one if it already exists
- Author `page--front.html.twig`: suppress page title, full-width `{{ page.content }}`
- Author sidebar documentation variant: `sidebar_first` region for TOC/index menu
- Wire up any new regions in `[theme].info.yml`
- Commit all templates and region additions with explicit paths
- Approval checkpoint before Phase 5

### Phase 5: Implementation Execution
- Author markup as SDC Twig templates — no loose HTML files
- Global CSS tweaks for existing native components → `css/base.css`
- Bespoke component CSS → encapsulated inside `components/[name]/[name].css`
  — NEVER in `base.css`
- Autonomous content via Heredoc stdin (no PHP files written to disk):
  ```bash
  cat << 'EOF' | [runtime_wrapper] drush scr -
  <?php
  // ... \Drupal::service('ai_agents') invocation ...
  EOF
  ```
- Commit SDC bundles and CSS wrappers

### Phase 6: Verification
- Manual hold: instruct user to `drush cr`, assemble in Canvas UI, provide URL
- Load URL in browser, visual regression against original screenshot
- Verify CSS cascade encapsulation
- **Failure path**: regression fails → `git revert HEAD` → return to Phase 5
  with failures documented as constraints

---

## Key Design Decisions

| Decision | Rationale |
|----------|-----------|
| Framework-agnostic master SOP | Must work for any base theme, not just Dripyard |
| Runtime auto-detected, never hardcoded | Prevents crashes on Lando, bare metal, etc. |
| Heredoc stdin for PHP payloads | `/tmp` not mounted in DDEV containers; zero disk pollution |
| SDC CSS always encapsulated locally | Prevents global cascade pollution in Canvas architecture |
| Git snapshot after every phase | Enables clean `git revert HEAD` for any single phase |
| Plan committed before approval halt | Prevents mapping plan floating un-tracked between conversation turns |
| `git add` always path-scoped | Prevents accidental staging of unrelated files |

---

## Active Project: Keytail Canvas Page

### Working Theme
`web/themes/custom/performant_labs_20260411`

### Design Assets
Located at: `web/themes/custom/performant_labs_20260411/designs/`

9 slices:
- `00_menu.webp` — Navigation bar (treated as its own unique slice)
- `01_hero.webp` — Hero banner
- `02_social_proof.webp` — Logo/brand row
- `03_features.webp` — Feature grid
- `04_dashboard.webp` — Dashboard preview
- `05_integrations.webp` — Integration logos
- `06_testimonials.webp` — Testimonials
- `07_cta.webp` — Call to action
- `08_footer.webp` — Footer

### Where We Left Off
Phases 1–5 complete and pushed. Theme is active, cache rebuilt.
Awaiting manual Canvas assembly in Drupal UI (Phase 6).

#### ⚠️ Critical Architecture Discovery
The home page is a **`canvas_page` entity** (Canvas module's own entity type),
NOT a standard Drupal node.

| Item | Value |
|---|---|
| Canvas home view | `/page/1` |
| Canvas home edit | `/page/1/edit` |
| Entity type | `canvas_page` (not `node`) |
| `page--front.html.twig` | ✅ Applies — fires when `is_front` is true regardless of entity type |
| `page--documentation.html.twig` | ✅ Applies to `book` nodes via theme suggestion hook |

---

## Immediate Next Steps for Next Session

1. **Canvas Assembly** (manual — Phase 6):
   - Log in at `https://pl-performantlabs.com.2.ddev.site:8493`
   - Navigate to `/page/1/edit`
   - Add Canvas sections in order per `drupal/dripyard_themes/theme_component_mapping_plan.md`
   - Provide assembled URL for visual regression

2. **Visual Regression** (Phase 6):
   - Load assembled page in browser
   - Compare against `designs/keytail-desktop.webp`
   - Verify CSS cascade is clean

3. **Remaining wiring** (after visual pass):
   - Wire `book_navigation` block to `sidebar_first` for book node routes
   - Create main nav items matching legacy site (5 items)
   - Wire footer menu + social-links to footer regions

---

# Session Summary — 2026-04-12
## Brand Palette Implementation, SOP Renumbering, Content Scrub

---

## What Was Accomplished

### 1. SOP Renumbering (1–9)
The master SOP was renumbered from 8 phases (with a confusing "1B" substep) to
a clean 1–9 sequential structure. All internal cross-references updated. Key
renaming: Legacy Audit became Phase 3, Canvas Assembly became Phase 7, Menu
Wiring became Phase 8, and Verification became Phase 9.

### 2. Brand Asset Phase (Phase 2 added)
A new **Phase 2: Brand Asset Collection** was inserted between pre-execution
discovery and theme cloning. It is now a hard gate — the SOP cannot proceed
until all six items are collected and verified:
- Color palette (all 6 brand hex values)
- Logo SVG (custom, verified via `curl`)
- Favicon
- Brand fonts
- Tagline / brand voice copy
- Primary and secondary hex values written to theme settings and verified via
  `curl | grep theme-setting-base-primary-color`

### 3. Performant Labs Brand Palette Applied

| Hex | Role |
|-----|------|
| `#F0F1F0` | `--white` \u2014 page surface / body background |
| `#1B2638` | Primary (navy) \u2014 theme settings, hero, footer |
| `#2D3E48` | `--theme-text-color-loud/medium` \u2014 headings + body |
| `#555F68` | `--theme-text-color-soft` \u2014 muted text, borders |
| `#F59E0B` | Secondary (amber) \u2014 CTAs, links |
| `#92600A` | `--theme-link-color-hover` \u2014 dark amber hover state |

Overrides scoped to `.theme--white` to preserve dark hero/footer zones.

### 4. Placeholder Content Scrub

10 Canvas components updated from Keytail/NeonByte demo copy to Performant
Labs copy using a single `drush scr` script with a keyed-replacement array.
Key changes:
- Hero h1: "Expert Drupal engineering, when you need it most."
- Features section: "Everything your team needs to ship faster."
- Section headings throughout updated from Keytail SaaS copy to Drupal/PL context

Pattern documented in `canvas-scripting-protocol.md` as "Keyed replacement
pattern for bulk content updates."

### 5. Logo Verified (Server-Side Only)

Logo SVG on disk confirmed correct (PL monogram, navy badge + amber P glyph).
`system.theme.global` confirmed `use_default: false` with correct custom theme
path. Browser continues to cache the NeonByte SVG \u2014 this is a client-side
HTTP cache issue, not a server problem. Resolves on hard reload. Added explicit
`curl` verification step to Phase 2 of the SOP so future sessions confirm
server-side state, not browser-rendered state.

---

## Key Learnings

| Learning | Where Documented |
|----------|-----------------|
| Logo SVG correct on disk \u2260 browser renders it; always confirm with `curl` | Phase 2 SOP, canvas-scripting-protocol Pre-flight #6 |
| Placeholder content passes visual regression without detection; scrub is mandatory | Phase 9.1 Content Audit (SOP), canvas-scripting-protocol Pre-flight #7, VR strategy |
| Palette belongs in Phase 2, not retrofitted at the end | Phase 2 SOP |
| `strip_tags()` required before keyed string replacement on Canvas text fields | canvas-scripting-protocol Keyed Replacement Pattern |
| Always verify CSS changes via `curl + grep` on the actual `<html>` style attribute, not by reading the CSS file | Phase 2 SOP verification step |

---

## Where We Left Off

All P0 and P1 visual gaps are resolved. The site is showing:
- Correct brand palette
- Correct PL section headings
- Correct hero heading and gradient
- Correct footer with social links

**Remaining P2 (cosmetic) gaps:**
1. Feature card body copy still Keytail ("AI Prospecting", "Automated Outreach")
2. Social links are text-only (not icon buttons)
3. Teams image is an Unsplash placeholder
4. Header scroll transparency needs JS listener verification

**Next step:** Begin Phase 9.1 Content Audit, then Phase 9.2 visual regression (one subagent per slice),
following the corrected protocol in `visual-regression-strategy.md`.
